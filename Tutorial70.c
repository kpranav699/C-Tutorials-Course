#include <stdio.h>
int main()
{
    // You have to create a command line utility to add/subtract/multiply/divide two numbers
    // First command line argument for your c program must be the operation.
    // The next arguments being the two numbers. For example:
    // >>Command.c add 45 4
    // >>49
    return 0;
}